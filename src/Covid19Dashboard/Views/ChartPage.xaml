<Page
    x:Class="Covid19Dashboard.Views.ChartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    xmlns:controls="using:Covid19Dashboard.Controls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}" Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
        <controls:ChartPageControl ChartIndicators="{x:Bind ViewModel.Source, Mode=OneWay}" ChartType="{x:Bind ViewModel.ChartType, Mode=OneWay}" />
    </Grid>
</Page>
